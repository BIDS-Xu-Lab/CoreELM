paths:
  dataset_subdir: dataset_chain1_reconstruct

prepare_graph_dataset:
  depth: 1
  tasks:
    - task_name: chain1_reconstruct
      include_target_embedding: true
      prompt_template: "The following papers form a citation chain in chronological order:{emb_token}{emb_token} Write the abstract of the most recent paper."

train:
  output_dir: experiment_output/chain1_reconstruct
