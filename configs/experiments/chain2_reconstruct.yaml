paths:
  dataset_subdir: dataset_chain2_reconstruct

prepare_graph_dataset:
  depth: 2
  tasks:
    - task_name: chain2_reconstruct
      include_target_embedding: true
      prompt_template: "The following papers form a citation chain in chronological order:{emb_token}{emb_token}{emb_token} Write the abstract of the most recent paper."

train:
  output_dir: models/chain2_reconstruct
