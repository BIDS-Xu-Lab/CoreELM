paths:
  dataset_subdir: dataset_chain_depth2

prepare_graph_dataset:
  depth: 2
  tasks:
    - task_name: chain_depth2
      prompt_template: "Three papers form a citation chain from oldest to most recent:{emb_token}{emb_token}{emb_token}. Write the abstract of the most recent paper."

train:
  output_dir: experiment_output/chain_depth2
