paths:
  dataset_subdir: dataset_chain_depth2_instruct

prepare_graph_dataset:
  depth: 2
  tasks:
    - task_name: chain_depth2_instruct
      prompt_template: "This line of research progressed through three works in sequence:{emb_token}{emb_token}{emb_token}. Write the abstract of the final paper in the chain."

train:
  output_dir: experiment_output/chain_depth2_instruct
