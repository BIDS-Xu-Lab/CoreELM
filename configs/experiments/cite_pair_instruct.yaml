paths:
  dataset_subdir: dataset_cite_pair_instruct

prepare_graph_dataset:
  depth: 1
  tasks:
    - task_name: cite_pair_instruct
      prompt_template: "Given a source paper {emb_token} and a paper that cites it{emb_token}, write the abstract of the citing paper."

train:
  output_dir: experiment_output/cite_pair_instruct
